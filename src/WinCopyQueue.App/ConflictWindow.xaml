<Window x:Class="WinCopyQueue.ConflictWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding [ConflictTitle]}"
        Width="680"
        Height="430"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#102033"
        Foreground="#EAF4FF">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#244D75" />
            <Setter Property="BorderBrush" Value="#4C7BA5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,9" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </Window.Resources>

    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding [ConflictHeading]}"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="White" />
        <TextBlock Grid.Row="1"
                   Text="{Binding [ConflictIntro]}"
                   Foreground="#9DB5C9"
                   FontSize="13"
                   Margin="0,5,0,16" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="#172C40" BorderBrush="#35516B" BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="{Binding [NewFile]}" Foreground="#62C3F4" FontSize="11" FontWeight="Bold" />
                    <TextBlock Text="{Binding SourceName}" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,3" />
                    <TextBlock Text="{Binding SourcePath}" Foreground="#7895AD" FontSize="10" TextTrimming="CharacterEllipsis" ToolTip="{Binding SourcePath}" />
                    <Separator Background="#35516B" Margin="0,14,0,12" />
                    <TextBlock Text="{Binding SourceSizeText}" FontSize="14" />
                    <TextBlock Text="{Binding SourceDateText}" Foreground="#B1C4D4" FontSize="12" Margin="0,7,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#172C40" BorderBrush="#35516B" BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="{Binding [ExistingFile]}" Foreground="#F0B95B" FontSize="11" FontWeight="Bold" />
                    <TextBlock Text="{Binding DestinationName}" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,3" />
                    <TextBlock Text="{Binding DestinationPath}" Foreground="#7895AD" FontSize="10" TextTrimming="CharacterEllipsis" ToolTip="{Binding DestinationPath}" />
                    <Separator Background="#35516B" Margin="0,14,0,12" />
                    <TextBlock Text="{Binding DestinationSizeText}" FontSize="14" />
                    <TextBlock Text="{Binding DestinationDateText}" Foreground="#B1C4D4" FontSize="12" Margin="0,7,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <CheckBox x:Name="ApplyToAllCheckBox"
                  Grid.Row="3"
                  Content="{Binding [ApplyAll]}"
                  Foreground="#C4D5E2"
                  Margin="2,16,0,15" />

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Content="{Binding [CancelSession]}"
                    Click="CancelSession_Click"
                    Background="#512A34"
                    BorderBrush="#9B4B59"
                    Margin="0" />
            <Button Grid.Column="2" Content="{Binding [Skip]}" Click="Skip_Click" Background="#344351" />
            <Button Grid.Column="3" Content="{Binding [Replace]}" Click="Replace_Click" Background="#1D6A4C" BorderBrush="#4BA679" />
        </Grid>
    </Grid>
</Window>
